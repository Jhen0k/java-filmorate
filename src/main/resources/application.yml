spring:
  sql:
    init:
      mode: never
  datasource:
    url: jdbc:h2:file:./db/filmorate
    username: sa
    password: password
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true